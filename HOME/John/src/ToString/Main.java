package ToString;

public class Main {

    public static void main(String[] args) {
        //  toString() specail method thet all object inherit,
        //that returns a string that" textually represent" an object.
        //can be used both implicitly=неявно and explicitly=явно

        Car car= new Car();
        //System.out.println(car.toString()); the same
        System.out.println(car);   // the same result
    }
}
